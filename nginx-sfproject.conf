# SF Project PDS Nginx Configuration
# This file routes domain names to the correct local ports

# PDS Service - pdsapi.sfproject.net -> localhost:2583
server {
    listen 80;
    server_name pdsapi.sfproject.net;
    
    location / {
        proxy_pass http://localhost:2583;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# AppView Service - bsky.sfproject.net -> localhost:2584
server {
    listen 80;
    server_name bsky.sfproject.net;
    
    location / {
        proxy_pass http://localhost:2584;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Ozone Service - ozone.sfproject.net -> localhost:3000
server {
    listen 80;
    server_name ozone.sfproject.net;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# PLC Service - plc.sfproject.net -> localhost:2582
server {
    listen 80;
    server_name plc.sfproject.net;
    
    location / {
        proxy_pass http://localhost:2582;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
